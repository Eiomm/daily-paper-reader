---
title: "AIM: A Privacy-Aware Interoperable Memory Framework for Multi-Agent Multi-User LLM Systems"
title_zh: AIM：面向多智能体多用户 LLM 系统的隐私感知可互操作记忆框架
authors: "Zachary Johnson, Nigel Boachie Kumankumah, Somya Chatterjee, Tejas Sathyamurthi, Min Chen, Xinyi Alice Li, Xiao Wang, Emily Morgan Gelchie, Jessica Lin, Sadid A. Hasan, Sulaiman Vesal"
date: 2026-09-11
pdf: "https://arxiv.org/pdf/2609.12320v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 面向多智能体多用户LLM系统的隐私感知可互操作记忆框架
tldr: "传统LLM记忆局限于单用户会话，难以共享公共知识并持续学习偏好。AIM提出面向多智能体多用户系统的隐私感知可互操作记忆框架，动态区分私有与公共信息并实施索引级访问控制。作者还发布MUMBench多用户记忆基准，覆盖检索、创建、更新与删除操作。三次运行中，AIM实现96.0%可见性分类准确率、58.8%严格操作准确率和70.5%状态感知操作准确率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.12320v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1383, \"height\": 836, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.12320v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1456, \"height\": 765, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12320v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1508, \"height\": 1237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12320v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 185, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12320v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12320v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1640, \"height\": 233, \"label\": \"Table\"}]"
motivation: 现有LLM记忆多局限于单用户，无法跨用户共享有益公共知识，也难持续跟踪演化偏好。
method: AIM动态将信息划分为仅本人可访问的私有记忆与全员可访问的公共记忆，并通过索引级访问控制保障隐私。
result: "在MUMBench三次独立运行中，AIM达96.0%可见性分类、58.8%严格操作和70.5%状态感知操作准确率。"
conclusion: AIM为多用户多智能体LLM提供隐私与共享兼顾的记忆方案，MUMBench填补多用户记忆操作评测空白。
---

## 摘要
传统大语言模型（LLM）局限于单个用户会话，其知识仅限于一次对话，无法学习随时间演变的用户偏好。现有的智能体记忆系统解决了这一局限，但通常仅在单用户层面运行，限制了可在用户间共享以改进下游响应的公共知识。我们提出 AIM（Agentic Interoperable Memory，智能体互操作记忆），一个统一的、隐私感知的记忆框架，使多智能体、多用户 LLM 系统能够持久地管理私有记忆与共享记忆。AIM 动态地将信息分类为私有——仅限某一用户访问且其他用户不可访问——或公共——所有用户均可访问。它实施索引级访问控制，使私有记忆仅能由其所有者检索，从而在保护敏感数据的同时，允许有益的共享知识提升协调性与一致性。我们还提出 MUMBench（Multi-User Memory Benchmark，多用户记忆基准），这是一个包含跨四个领域的私有信息与可共享信息的多用户交互数据集。据我们所知，MUMBench 是首个旨在评估多用户环境中多种记忆操作的公开数据集，这些操作包括检索、创建、更新和删除。在 MUMBench 上的三次独立运行中，AIM 实现了 96.0% 的可见性分类准确率、58.8% 的严格操作准确率和 70.5% 的状态感知操作准确率。

## Abstract
Traditional large language models (LLMs) are scoped to individual user sessions, limiting their knowledge to a single conversation and preventing them from learning user preferences that evolve over time. Existing agentic memory systems address this limitation but generally operate at the individual-user level, restricting the public knowledge that could be shared across users to improve downstream responses. We introduce AIM (Agentic Interoperable Memory), a unified, privacy-aware memory framework that enables multi-agent, multi-user LLM systems to persistently manage private and shared memory. AIM dynamically classifies information as private, scoped to one user and inaccessible to others, or public, accessible to all users. It enforces index-level access controls so that private memories are retrievable only by their owner, protecting sensitive data while allowing beneficial shared knowledge to improve coordination and consistency. We also introduce MUMBench (Multi-User Memory Benchmark), a dataset of multi-user interactions containing private and shareable information across four domains. To our knowledge, MUMBench is the first public dataset designed to evaluate multiple memory operations, including retrieval, creation, update, and deletion, in a multi-user environment. Across three independent runs on MUMBench, AIM achieves 96.0% visibility classification accuracy, 58.8% strict operation accuracy, and 70.5% state-aware operation accuracy.

---

## 论文详细总结（自动生成）

# AIM：面向多智能体多用户 LLM 系统的隐私感知可互操作记忆框架——论文总结

## 1. 核心问题与整体含义

- **研究背景**：传统 LLM 被限定在单个用户会话内，知识仅来自当前对话，无法跨会话、跨用户持续学习演变的偏好，也难以在多智能体、多用户场景中共享公共知识。
- **核心问题**：现有智能体记忆方案大多面向单智能体、单用户，缺少多用户访问控制、跨智能体同步、私有/公共记忆隔离，导致重复交互、上下文碎片化、个性化受限，并存在隐私泄露风险。
- **整体含义**：论文提出 **AIM（Agentic Interoperable Memory）**，一个统一、隐私感知、可互操作的记忆框架，使多智能体、多用户 LLM 系统能够持久管理私有与共享记忆。
- **关键目标**：
  - 动态区分 **PRIVATE（仅所有者可访问）** 与 **PUBLIC（所有用户可访问）** 记忆。
  - 在检索索引层面强制访问控制，而非仅依赖概率性过滤。
  - 支持完整记忆生命周期：创建、读取、更新、删除、去重与生命周期管理。
  - 发布 **MUMBench**，首个面向多用户、隐私感知、多操作记忆评测的公开基准。

## 2. 方法论

### 2.1 核心思想

- AIM 作为可移植、系统无关的服务运行，多个异构智能体可共享同一记忆注册表，同时遵守用户级隐私约束。
- 系统分为两条级联推理路径：
  - **写路径**：操作检测 → 结构化提取 → 可见性分类 → 语义去重 → 持久化。
  - **读路径**：可见性约束稠密检索 → 标签重打分 → LLM 重排 + 时间融合。
- 核心约束是可见性谓词 \(V(u,m_i)\)，贯穿读写全流程。

### 2.2 问题形式化

- 用户集合 \(U\)、智能体集合 \(A\)、持久记忆库 \(M=\{m_1,m_2,\dots\}\)。
- 每条记忆包含：文本内容 \(c_i\)、稠密嵌入 \(e_i\in\mathbb{R}^d\)、所属用户 \(u_i\)、智能体 \(a_i\)、可见性 \(v_i\in\{\text{PUBLIC},\text{PRIVATE}\}\)、标签集 \(\tau_i\)、时间戳 \(t_i\)、元数据 \(\mu_i\)。
- 可见性谓词：
  - \(V(u,m_i)=\text{true}\) 当且仅当 \(v_i=\text{PUBLIC}\)，或 \(v_i=\text{PRIVATE}\) 且 \(u_i=u\)。
  - 即：公共记忆所有人可读；私有记忆仅所有者可读，且无论由哪个智能体创建。
- 写操作额外要求：更新与删除仅允许创建者用户或创建者智能体执行，防止跨用户覆盖。

### 2.3 多标签意图检测

- 将用户消息 \(x\) 映射为操作子集：\(\{C,R,U,D\}\) 或 NONE。
- 支持 **多标签**：例如“我搬到旧金山了，能推荐晚餐吗？”同时触发 `{CREATE, READ}`。
- 通过上下文学习实现：提示中包含操作分类体系、少样本示例、隐私守卫；约束解码确保输出合法标签。

### 2.4 结构化记忆提取

- 当检测到写操作（C 或 U）时，提取结构化记忆元组 \(\{(c_j,\tau_j,\mu_j)\}\)。
- 输出 JSON 模式：文本内容、关键词标签、可选来源元数据（时间上下文、领域类别）。
- 使用受约束 JSON 模式解码。

### 2.5 隐私感知可见性分类

- 分类器 \(f_{\text{cls}}\) 输入记忆内容与元数据，输出 PUBLIC 或 PRIVATE。
- 提示中编码归纳偏置：
  - **公共信号**：集体事件、团队会议、截止日期、组织公告、共享资源。
  - **私有信号**：个人偏好、个人日程、直接消息、健康/财务等敏感数据。
- 该分类影响去重范围、检索过滤、更新/删除权限。

### 2.6 上下文语义去重

- 目标：避免冗余与矛盾记忆降低检索精度。
- 给定候选记忆，先构造 **范围受限比较集 \(M_v\)**：
  - 公共记忆：与所有公共记忆比较。
  - 私有记忆：仅与同一用户的私有记忆比较，防止跨用户信息泄露。
- 三分类结果：
  - **CREATE**：无等价记忆。
  - **UPDATE**：存在同主题但过时记忆，返回指向 \(\hat{m}\) 的指针。
  - **NO-OP**：已有记忆覆盖该内容。
- 通过上下文学习实现，将比较集序列化后一次前向评估语义重叠与时间替代
