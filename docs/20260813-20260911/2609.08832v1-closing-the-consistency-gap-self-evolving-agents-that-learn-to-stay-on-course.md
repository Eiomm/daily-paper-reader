---
title: "Closing the Consistency Gap: Self-Evolving Agents That Learn to Stay on Course"
title_zh: 弥合一致性差距：学习保持正轨的自进化智能体
authors: "Evelyn Duesterwald, Benjamin Elder, Lilian Ngweta, Shashanka Ubaru, Malgorzata Zimon"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.08832v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 自演化智能体将不稳定步骤转化为情景记忆
tldr: "LLM智能体平均准确率尚可，但同一任务多次执行结果不稳定：ReAct在AppWorld上用GPT-4.1单次通过率77%，五次全成功仅53%，形成24个百分点的‘一致性差距’。本文提出自进化智能体框架，定位轨迹中易翻转的低一致性步骤，并生成针对性指南写入情景记忆，供未来相似任务调用。在AppWorld上，五次全成功率同任务提升16个百分点，相似任务泛化提升13个百分点。该工作将一致性缺口转化为可优化目标，推动智能体在生产环境中可信部署。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.08832v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08832v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1763, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.08832v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1727, \"height\": 483, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.08832v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1671, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.08832v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1650, \"height\": 546, \"label\": \"Table\"}]"
motivation: LLM智能体单次准确率尚可，但同一任务重复执行时结果波动大，形成一致性差距，阻碍生产环境可信部署。
method: 提出自进化框架：一致性分析器定位轨迹中易翻转的低一致性步骤，指南生成器将诊断转为针对性指南，写入情景记忆并注入未来相似任务。
result: 在AppWorld上，ReAct/GPT-4.1五次全成功率同任务提升16个百分点，相似任务泛化提升13个百分点。
conclusion: 该工作把一致性缺口作为可优化目标，通过记忆化指南提升智能体执行稳定性，为可信智能体部署提供新路径。
---

## 摘要
由大型语言模型（LLM）驱动的智能体平均而言可以很准确，但在生产环境中却不可靠；这种差异已被观察到，但很大程度上仍未得到解决。在 AppWorld 基准上，使用 GPT-4.1 的 ReAct 智能体在相同任务上运行五次时，五次全部成功的比例仅为 53%，尽管其单次运行的平均通过率为 77%。我们将这 24 个百分点的差距称为一致性差距，并认为解决该差距是可信 AI 智能体部署的前提条件。我们提出一个自进化智能体框架，通过识别智能体轨迹中不稳定、低一致性的步骤，并将其转化为智能体在未来运行中可以利用的情景记忆，从而缩小这一差距。其核心是一个一致性分析器，用于精确定位轨迹在多次执行中可能发生翻转的位置及原因；以及一个指南生成器，用于将诊断结果转化为有针对性的指南，写入记忆，并注入未来在相似任务上的智能体执行中。在 AppWorld 上，使用 ReAct/GPT-4.1 时，我们的框架将五次运行全部成功的任务比例在相同任务评估中提高了 16 个百分点，在相似任务泛化中提高了 13 个百分点。

## Abstract
Large language model (LLM)-powered agents can be accurate on average yet unreliable in production, a discrepancy that has been observed but remains largely unaddressed. When given the same task five times, a ReAct agent on the AppWorld benchmark using GPT-4.1 succeeds in all five runs only 53% of the time, even though its per-run pass rate averages 77%. We call this 24-point shortfall the consistency gap, and we argue that addressing it is a precondition for trustworthy AI agent deployment. We present a self-evolving agent framework that reduces this gap by identifying unstable, low-consistency steps in agent trajectories and converting them into episodic memory the agent can draw on in future runs. At its core is a Consistency Analyzer that pinpoints where and why a trajectory is likely to flip across executions, and a Guideline Generator that converts the diagnosis into targeted guidelines, committed to memory and injected into future agent executions on similar tasks. On AppWorld with ReAct/GPT-4.1, our framework raises the fraction of tasks that succeed in all five runs by +16 points on same-task evaluation and +13 points on similar-task generalization.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **核心问题**：LLM 智能体在基准上“平均准确率”尚可，但同一任务重复执行时结果不稳定，形成“一致性差距”（consistency gap）。例如 ReAct + GPT-4.1 在 AppWorld 上 `Mean@5 = 77%`，但 `Pass^5` 仅 `53%`，差距达 `24.4` 个百分点。
- **整体含义**：论文认为，生产部署所需的可信性不仅取决于平均能力，还取决于可重复性；若不解决一致性差距，智能体在真实场景中会表现为“平均能过、实际常翻车”。
- **目标**：把一致性作为一等优化目标，通过自进化记忆机制，让智能体在未来相似任务中减少低一致性、易翻转步骤。

## 2. 方法论

- **核心思想**：离线检测轨迹中“易翻转”的低一致性步骤，将其转化为自然语言“一致性指南”，写入情景记忆，并在未来相似任务执行时注入提示词，从而稳定推理点。
- **整体流程**：
  - **操作循环**：智能体执行任务，可检索记忆中的指南，记录完整轨迹。
  - **离线分析循环**：`Detect` 阶段用一致性分析器定位问题；`Generate` 阶段生成指南；指南进入双索引情景记忆，后续按余弦相似度或 LLM 选择器检索。
- **一致性分析器（Consistency Analyzer）**：
  - 黑盒方式：只重新调用同一 LLM 端点，不需要 logits、内部激活或智能体源码。
  - 对每个推理步骤的 prompt 重采样 `N` 次，默认 `N=30`、温度 `0.5`。
  - 步骤一致性 `C(s_i) ∈ [0,1]` 衡量响应分布的集中程度：
    - **自由文本/代码**：用 SBERT 嵌入的平均成对余弦相似度。
    - **类别型响应**：如工具名、枚举参数，用平均成对 Jaccard 相似度。
    - **结构化响应**：如 JSON，按字段分解后加权求和。
  - 轨迹一致性：`C_mean(T)` 为各步骤一致性均值。
- **指南生成（Guideline Generator）**：
  - 低于阈值 `θ_C = 0.85` 且位于决策路径上的步骤被标记为不一致。
  - LLM 根据带标记轨迹和一致性评分卡生成指南，类别包括：
    - **strategy**：强化“虽然高变异但正确”的模式。
    - **recovery**：绕开与失败相关的高变异步骤。
    - **optimization**：跳过或简化不必要的高变异步骤。
  - 指南存入情景记忆，带类别、优先级、上下文、任务类别、来源等元数据，并支持去重、聚类、合并。
- **关键点**：该方法不改变推理循环，架构无关，仅在 prompt 层注入短期自然语言指南。

## 3. 实验设计

- **数据集/场景**：AppWorld `test normal` split，共 `168` 个任务，按难度分为 Easy `57`、Medium `48`、Hard `63`。
- **智能体与模型**：ReAct 智能体，分别由 GPT-4.1 和 GPT-OSS-120B 驱动。
- **评价指标**：
  - `Pass@k`：k 次中至少成功一次。
  - `Mean@k`：k 次平均通过率。
  - `Pass^k`：k 次全部成功。
  - 一致性差距 `Gap@k = Mean@k - Pass^k`。
  - 归一化一致性 `Consistency@k = Pass^k / Mean@k`。
- **实验设置**：
  - 每个任务运行 `5` 次，报告 `Pass^5`、`Mean@5` 等。
  - **同任务评估**：从每任务单条基线轨迹生成指南，再在同任务重复运行中注入。
  - **相似任务泛化**：AppWorld 每个 scenario 有 `v1/v2/v3` 三个相似变体，从 `v1` 生成指南并注入 `v2/v3`，共 `336` 次评估。
  - **轨迹一致性预测能力**：在 `50` 个 AppWorld challenge 任务上，用单条 ReAct/GPT-4.1 轨迹，计算聚合一致性的 AUROC。
- **对比对象**：主要对比无指南基线、同任务指南、相似任务指南；未与其它记忆方法或一致性方法做直接横向对比。
- **模型端点**：GPT-4.1 在 Microsoft Azure，GPT-OSS-120B 在 AWS。

## 4. 资源与算力

- 论文未明确给出 GPU 型号、数量、训练时长、能耗或费用。
- 方法不涉及模型训练或微调，主要成本来自云托管 LLM 推理与重采样。
- 默认重采样预算 `N=30`，每个推理步骤约带来 `30×` token 成本。
- 文中提到未来需要降低该成本，例如自适应重采样，但未提供具体算力统计。

## 5. 实验数量与充分性

- **实验规模**：
  - 两个模型：GPT-4.1、GPT-OSS-120B。
  - 两种设置：同任务、相似任务。
  - 四个层级：Aggregate、Easy、Medium、Hard。
  - 相似任务共 `336` 次评估。
  - 另有 `50` 个 challenge 任务用于 AUROC 分析。
- **充分性**：
  - 覆盖了不同能力模型、不同难度、同任务与相似任务泛化，初步验证有效。
  - 但缺少关键消融实验：如阈值 `θ_C`、重采样预算 `N`、不同一致性度量、指南验证机制、记忆检索策略等。
  - 未与其他一致性提升方法、自一致性、智能体记忆方法进行直接对比。
  - 未报告统计显著性、置信区间或多次实验方差。
- **公平性风险**：
  - 指南由同一模型基于自身轨迹生成并评判，可能存在自偏好或循环偏差。
  - 平台侧非确定性无法直接测量和控制，基线运行与指南注入运行的非确定性被假设可比。
  - 样本预算敏感性研究在贡献中被提及，但正文未充分展开。

## 6. 主要结论与发现

- **一致性差距显著**：
  - GPT-4.1：`Mean@5=77.4%`，`Pass^5=53.0%`，`Consistency=0.68`，差距 `24.4 pp`。
  - GPT-OSS-120B：`Mean@5=33.9%`，`Pass^5=10.1%`，`Consistency=0.30`，差距 `23.8 pp`。
- **同任务提升**：
  - GPT-4.1：`Pass^5` 从 `53.0%` 升至 `69.0%`，`+16.0 pp`；`Mean@5` 从 `77.4%` 升至 `81.0%`，`+3.6 pp`；一致性从 `0.68` 升至 `0.85`。
  - GPT-OSS-120B：`Pass^5` 从 `10.1%` 升至 `16.1%`，`+6.0 pp`；`Mean@5` 从 `33.9%` 升至 `38.7%`，`+4.8 pp`；一致性从 `0.30` 升至 `0.42`。
- **相似任务泛化**：
  - GPT-4.1：`Pass^5` 提升 `+13.0 pp`，`Mean@5` 提升 `+2.1 pp`，一致性达 `0.83`。
  - GPT-OSS-120B：`Pass^5` 提升 `+8.7 pp`，`Mean@5`
